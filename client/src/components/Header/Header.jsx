import './Header.css'
import React from 'react';

const Header = () => {
    return (
        <div className="grid">
            <button>Match</button>
            <button>Profile</button>
       </div>
    );
};

export default Header
